/**
 * Base URL of the House Points API. A single constant for now (per
 * `front/ARCHITECTURE.md`); every repository builds full request URLs off it.
 */
const val API_BASE_URL = "http://localhost:8080/api"
